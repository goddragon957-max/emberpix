# Emberpix 골모드 프롬프트 (M17 → M19 완주용)

아래 블록을 그대로 복사해 새 세션에 붙여 넣으면 된다.

---

## 붙여넣을 프롬프트

너는 Emberpix(브라우저 픽셀아트 에디터) 프로젝트를 골모드로 이어서 완주한다.
작업 디렉터리: `C:\ProjectsSY\emberpix`

### 1. 먼저 읽어라 (순서대로)

1. `AGENTS.md` — 룰 정본
2. `ROADMAP.md` — 방향
3. `실행계획서-v4.md` — **실행 순서·완료 기준 (이 문서가 지시서다)**
4. `.agents/skills/canvas-editor/SKILL.md` — 캔버스/입력/히스토리 패턴과 함정

### 2. 현재 상태 (2026-08-06 기준)

- 브랜치 `master`, working tree 깨끗. 원격 `github.com/goddragon957-max/emberpix` (public)
- **완료: M1~M16.** M9~M13은 `실행계획서-v3.md`, M14~M16은 실사용 피드백에서 나와
  계획서 없이 진행됐다. 정확한 내용은 `git log --oneline`이 정본이다.
- **실사용자는 폰을 쓰는 아이들이다.** 기능을 더 넣는 것보다 "지금 쓸 것만 크게 보이기"가 우선.
- 남은 작업: **M17 → M18 → M19** (실행계획서-v4.md의 §1, §2, §3)

### 3. 이번 실행 범위

**M17 → M18 → M19를 순서대로 끝까지 진행한다.** 각 마일스톤마다:

1. 구현
2. 순수 로직은 `node`로 단위 테스트 (결정론적이고 빠르다)
3. `npm run build`로 빌드 통과 확인
4. 실행계획서 §5 회귀 체크리스트 확인
5. **마일스톤 단위로 한국어 커밋** (계획서에 적힌 커밋 메시지 사용) 후 푸시
6. 계획서 §6 보고 형식(한국어 3~5줄)으로 보고한 뒤 **다음 마일스톤으로 바로 넘어간다**

마일스톤 사이에서 승인을 기다리지 말고 계속 진행한다. 단, 아래 §6 중단 조건에 걸리면 멈추고 물어라.

### 4. 이미 내려진 결정 (다시 묻지 마라)

- **npm 의존성 추가 금지.** GIF 인코더(`src/core/gif.js`)도 컨페티(`src/core/confetti.js`)도
  직접 구현했다. 필요하면 같은 방식으로 직접 만든다. 예외는 GitHub 공식 액션뿐.
- **네이티브 앱 변환은 보류.** PWA로 간다. 스토어 배포가 실제로 필요해지면 그때 재검토.
- 시작 화면에서 모드 카드 2개로 진입. **보석 모드는 펜 고정**(지우개도 없음).
- 고급 기능은 "더보기" 서랍에. **페이지는 절대 스크롤되지 않는다.**
- 확대 배율 하한은 1×.

### 5. 이 환경의 검증 방법 (중요 — 시간 낭비 방지)

- **스크린샷 도구를 쓰지 마라.** `computer{action:"screenshot"}`은 30초 타임아웃된다.
- 브라우저 검증은 `mcp__Claude_Browser__javascript_tool`로 한다. **`tabId` 인자 필수.**
- **포트 5199는 다른 앱이 점유 중이다.** `.claude/launch.json`의
  `emberpix-dev-alt`(5210, dev) / `emberpix-preview`(5211, 프로덕션 빌드)를 쓴다.
- **브라우저 pane이 `hidden`이거나 접혀 0×0일 수 있다.** 이때:
  - `requestAnimationFrame`이 **아예 안 돈다** → 애니메이션(컨페티) 확인 불가
  - `setTimeout`이 ~4초로 스로틀된다 → `await new Promise(r=>setTimeout(r,50))`이 4초 걸린다
  - `ResizeObserver` 콜백이 배달되지 않는다
  - `getComputedStyle`이 호출당 수백 ms로 느리다 → React는 인라인 style을 쓰니
    `el.style.background === 'rgb(255, 122, 47)'`로 읽어라
  - 먼저 `document.visibilityState`와 `[innerWidth, innerHeight]`를 확인하고 시작하라
- **React 상태 반영은 마이크로태스크로 기다린다**: `for (let i=0;i<12;i++) await Promise.resolve()`.
  `useEffect`(캔버스 재렌더)는 태스크라 지연되므로, 렌더 결과 샘플링은 **다음 도구 호출**에서 하라.
- **0×0일 때 포인터 검증을 살리는 법**: `#root`와 `html/body`에 크기를 강제하면
  `getBoundingClientRect()`가 살아난다.
  ```js
  const r = document.getElementById('root');
  r.style.width='390px'; r.style.height='780px'; r.style.position='relative';
  document.documentElement.style.width='390px'; document.documentElement.style.height='780px';
  document.body.style.width='390px'; document.body.style.height='780px';
  ```
- **파일 입력 검증**: `<input type=file>`이 여러 개라 DOM 순서로 고르면 틀린다.
  `HTMLInputElement.prototype.click`을 잠깐 가로채 버튼이 실제로 여는 input을 잡고
  `DataTransfer`로 File을 넣어라.
- **다운로드 검증**: `HTMLAnchorElement.prototype.click`을 가로채 `download`/`href`를 잡으면
  실제 저장 없이 `fetch(href)`로 내용까지 검사할 수 있다.
- 캔버스는 `ctx.getImageData`로 셀 중앙 샘플. `#ef7d57` = `[239,125,87]`,
  체커 배경 `[35,37,45]`/`[27,29,35]`. 오버레이(선택 10% 틴트, 도형 미리보기 0.8,
  보석 가이드 0.3 / 필터 0.55·0.1)가 색을 바꾸니 순수 색은 Esc 후 샘플.
- **`git commit`이 2분을 넘길 수 있다.** Bash 타임아웃을 240000ms로 잡아라.
  타임아웃이 나도 뒤늦게 성공할 수 있으니 재시도 전 `git log --oneline -3`으로 확인.
- **푸시하려면 gh 계정을 바꿔야 한다.** 활성 계정이 `Dragon80741`이면 403이 난다.
  ```
  gh auth switch --user goddragon957-max && git push; gh auth switch --user Dragon80741
  ```
- 검증 불가한 항목이 있으면 **통과했다고 쓰지 말고, 무엇을 왜 못 했는지 정직하게 보고하라.**

### 6. 중단 조건 (여기서만 멈추고 물어라)

- 의존성을 추가해야만 풀리는 문제에 부딪힌 경우
- 저장소 설정 변경이 필요한 경우 (GitHub Pages 소스 지정 등) — 사용자가 직접 해야 한다
- 회귀 체크리스트가 깨지고 원인을 못 찾은 경우
- 계획서의 완료 기준을 만족시킬 수 없어 범위를 줄여야 하는 경우
- 파일/폴더를 삭제하거나 덮어써야 하는 경우 — 범위를 좁게 확인받은 뒤 실행

### 7. 반드시 지킬 것

- 답변·보고·커밋 메시지는 **한국어**
- 마일스톤 건너뛰기/병합 금지, 계획서 순서 준수
- 디자인 토큰 고정: 배경 `#141519` / 패널 `#1e2027` / 액센트 `#ff7a2f`
- 외부 UI 라이브러리 금지, 스타일 직접 작성
- 파일당 하나의 책임: 알고리즘은 `src/core/*`, `App.jsx`는 UI·입력만
- 픽셀 좌표는 `(x, y)`, 인덱스는 `y * size + x`
- **undo 스냅샷 대상은 "현재 프레임"** (프레임별 독립 히스토리)
- 스트로크 = down에서 `pushUndo` 1회 → move에서 paint → up에서 종료.
  도형은 up에서 `pushUndo` 1회 후 확정
- **확정 전 미리보기는 절대 픽셀 데이터를 건드리지 않는다** (renderer 오버레이로만)
- 좌표 계산에 `Infinity`/`NaN` 가드를 넣어라 (무한 루프 방지)
- 멀티터치: 포인터 2개 이상이면 진행 중 조작을 취소하고 확대/이동 제스처로 전환
- **새 내장 도안은 16/32/64 전부에서 가장자리에 닿지 않는지 확인하라**
  (하트가 잘려 있던 전례 — `patterns.js`의 HEART_* 상수 참고)
- **CSS `aspect-ratio` + `max-height` 조합을 믿지 마라.** 가로가 긴 화면에서 폭이 줄지 않아
  직사각이 된다. 정사각이 필요하면 `ResizeObserver`로 실측해 직접 지정한다(이미 그렇게 돼 있다).

### 8. 시작

`M17`(배포)부터 시작해라. 실행계획서-v4.md §1을 그대로 따르고, 끝나면 곧바로 M18 → M19로 이어가라.
M17에서 **컨페티가 실제로 날아가는 것을 눈으로 확인**하는 게 숙제로 남아 있다
(이전 세션에선 pane이 hidden이라 rAF가 안 돌아 확인하지 못했다).
