# Emberpix 골모드 프롬프트 — M21 개인정보 최소화 공개 베타 준비

아래 블록을 새 골모드 세션에 그대로 붙여 넣는다.

---

## 붙여넣을 프롬프트

너는 Emberpix의 M21(개인정보 최소화 공개 베타 준비)을 자율적으로 진행하는 코딩 에이전트다.

작업 경로: `C:\ProjectsSY\emberpix`

### 먼저 읽어라

1. `AGENTS.md`
2. `ROADMAP.md`
3. `실행계획서-v6.md` — M21의 정본
4. `git status`, `git log --oneline -20`

### 현재 상태

- M1~M20 완료. M20에서 Galaxy S24 Chrome 세로·가로 핵심 흐름은 실기기로 확인했다.
- 현재 제품은 GitHub Pages용 Vite PWA이며, 계정·서버·광고·분석 SDK 없이 작품을 기기
  `localStorage`에 보관한다.
- M21의 첫 고객은 부모·교사이며, 아이는 사용자다. 첫 배포는 무로그인 웹 PWA 베타다.
- `.omx/`는 사용자/세션 산출물일 수 있으므로 수정·삭제·스테이지하지 마라.

### 진행 방식

1. 먼저 Node 테스트와 `npm run build`를 확인하고, 현재 배포·저장·외부 데이터 수집 여부를 점검한다.
2. 운영 주체명, 공개 문의 이메일, 도메인, 부모·교사 피드백 채널과 수집 항목이 정해지지 않았다면
   실제 공개 화면·개인정보 수집·결제를 구현하지 말고 필요한 결정만 짧게 요청한다.
3. 승인된 정보가 주어졌다면 개인정보 최소화 안내, 부모·교사 피드백 흐름, 작품 기기 저장·백업 안내를
   기존 키즈 UI를 해치지 않는 최소 범위로 구현한다.
4. 광고, 회원가입, 아동 직접 피드백, 외부 추적 SDK, 결제, 클라우드 저장은 M21에 추가하지 않는다.
5. 부모·교사 20명 베타 결과를 기기/브라우저/사용 흐름/재방문/유료 의향/재현 이슈로 기록한다.
6. 재현 이슈만 수정하고, 변경마다 관련 테스트·`npm run build`·브라우저 검증을 한다.
7. M21 완료 기준이 모두 충족될 때만 다음 제목의 한국어 Lore Commit Protocol 커밋을 만들고 `master`에 푸시한다.

`마일스톤 21: 개인정보 최소화 공개 베타 준비`

### 반드시 보존할 것

- 디자인 토큰 `#141519` / `#1e2027` / `#ff7a2f`
- 직접 canvas 렌더링, Pointer Events, 프레임별 snapshot undo
- 단축키 B/E/G/I, Ctrl+Z, Ctrl+Shift+Z
- 자동저장, 작품 보관함, `.emberpix` 호환성, GitHub Pages 상대 경로
- 페이지 자체 스크롤 금지(서랍·가로 목록만 스크롤)

### 중단 조건

- 운영 주체·문의처·도메인·피드백 수집 범위가 확정되지 않아 실제 공개 문구를 만들 수 없는 경우
- 결제, 개인정보, 광고, 외부 SDK, 저장 포맷, 배포 설정 변경이 필요한 경우
- 실제 베타 결과나 재현 절차 없이 기능을 추측으로 추가해야 하는 경우

중단 조건이면 코드를 바꾸지 말고, 필요한 결정 또는 관찰만 짧게 요청한다.
