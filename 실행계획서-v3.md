# Emberpix v3 실행 계획서 (골 모드)

> 골모드 실행용 계획서. **AGENTS.md(룰) → ROADMAP.md(방향) → 본 문서(실행 순서·완료 기준)** 순으로 읽고 착수한다.
> v1/v2 로드맵(M1~M7)과 보석십자수(M8)는 완료 상태. 본 문서는 그 위에 얹는 **v3(M9~M13)** 이다.
> 마일스톤 단위로만 커밋하고, 순서를 임의로 건너뛰거나 병합하지 않는다.

## 0. 사전 확인 (착수 전 1회)

- [ ] AGENTS.md, ROADMAP.md, .agents/skills/canvas-editor/SKILL.md 읽기
- [ ] `npm install && npm run dev` → http://localhost:5199 에서 현재(M8)까지 정상 동작 확인
- [ ] 아래 §6 회귀 체크리스트 전 항목 통과 확인 (기준선 확보)
- [ ] 코어 구조 파악: `src/core/*`(grid/history/renderer/exporter/storage/format/project/selection/reference/templates/patterns) + `src/App.jsx`(UI·입력만)

**검증 방법 주의(이 환경 필수):** 스크린샷 도구는 30초 타임아웃되므로 쓰지 않는다.
`javascript_tool`(tabId 필수)로 검증한다 — 캔버스는 `ctx.getImageData`로 셀 중앙 색 샘플링, UI는 DOM/`getComputedStyle`, 조작은 합성 PointerEvent. 순수 로직(좌표변환/도형/양자화/GIF 등)은 `node --input-type=module`로 결정론적 단위 테스트하는 게 더 빠르고 확실하다.

---

## 1. 마일스톤 9 — 캔버스 확대/이동 (pan & zoom)  ⟶ 토대

**목표**: 큰 캔버스도 폰에서 한 픽셀씩 편하게. 이후 모든 도구가 얹히는 좌표계 토대라 **가장 먼저** 한다.

**작업 순서**
1. 뷰 상태 도입: `scale`(배율), `offset{x,y}`(픽셀 이동). ref 기반, 리렌더는 version 카운터 유지.
2. **좌표계 통일이 핵심**: `cellFromEvent`가 scale/offset을 역변환해 셀을 구하도록 수정. SKILL.md의 `getBoundingClientRect` 비율 계산에 뷰 변환을 합성한다. (여기서 어긋나면 전 도구가 틀어짐)
3. 표시: 캔버스 컨테이너에 CSS transform(`translate`+`scale`) 또는 내부 렌더 오프셋. `image-rendering: pixelated` 유지, 확대해도 선명.
4. 데스크톱: 휠 확대(커서 기준 앵커), 스페이스+드래그 또는 중간버튼으로 팬.
5. 터치: **두 손가락 핀치 = 확대, 두 손가락 드래그 = 팬, 한 손가락 = 그리기**. 포인터가 2개 이상이 되면 진행 중인 스트로크를 취소하고 제스처 모드로 전환(중요 함정).
6. "화면 맞춤(리셋)" 버튼 + 현재 배율 표시. 배율 범위 예: 0.5×~16×.

**완료 기준**
- 폰에서 64×64를 확대해 원하는 픽셀 하나만 정확히 찍힘
- 핀치/팬이 자연스럽고, 그리기 좌표가 배율·오프셋과 무관하게 정확
- 확대 상태에서도 그리드/보석 렌더 정상
- §6 회귀 통과 (특히 멀티터치 중 오작동 없음)

**커밋**: `마일스톤 9: 캔버스 확대/이동 (핀치줌·팬, 좌표계 통일)`

---

## 2. 마일스톤 10 — 그리기 도구 확장

**목표**: 도형 도구 + 브러시 크기 + 색 교체로 스프라이트 제작 정밀도 향상.

**작업 순서**
1. `src/core/shapes.js` 신규: 직선(브레젠험), 사각형(테두리/채움), 원/타원(중점 원). 모두 순수 함수, 셀 좌표 목록 반환.
2. 도형 도구 입력: down에서 시작점, move에서 **오버레이 미리보기**(픽셀 데이터 오염 금지 — renderer의 selection 오버레이처럼 최상단에 임시 렌더), up에서 확정(pushUndo 1회).
3. 브러시 크기(1~4): 펜/지우개에 적용(정사각 브러시). 대칭 그리기와 호환.
4. 색 교체(전역): 클릭한 셀의 색과 같은 모든 셀을 현재 색으로 치환(현재 프레임, undo 대상).
5. 단축키 배정(기존 B/E/G/I/M과 겹치지 않게): 예 L 직선 / R 사각 / O 원 / +,- 브러시.

**완료 기준**
- 각 도형이 의도한 픽셀로 정확히 확정, 미리보기가 데이터 오염 없이 오버레이
- 브러시 크기·대칭 조합 정상, 색 교체 정확
- 확대 상태(M9)에서도 도형 좌표 정확
- §6 회귀 통과

**커밋**: `마일스톤 10: 도형 도구·브러시 크기·색 교체`

---

## 3. 마일스톤 11 — 팔레트 관리

**목표**: 커스텀 팔레트 저장/전환 + 이미지에서 팔레트 추출.

**작업 순서**
1. `src/core/quantize.js` 신규: 이미지 → 대표색 N개 추출(미디언 컷 또는 k-means 근사). **M12 도안 생성과 공유하는 핵심 유틸**이니 여기서 견고하게.
2. 팔레트 슬롯: 여러 팔레트 저장/이름/전환/삭제. 최근 사용 색 자동 기록(예: 최근 12색).
3. 커스텀 색 추가/삭제 UI. 기본 Sweetie 16은 유지.
4. 저장 포맷: 이미 `palette` 필드 존재 → 다중 팔레트로 확장(하위호환 유지, format.js 검증 추가). 자동저장/.emberpix 모두 반영.

**완료 기준**
- 팔레트 저장/전환/삭제, 최근 색 기록 정상
- 이미지에서 팔레트 추출(대표색 N개) 동작
- 새로고침·프로젝트 파일 저장/불러오기로 팔레트 복원
- §6 회귀 통과

**커밋**: `마일스톤 11: 커스텀 팔레트 관리 및 이미지 팔레트 추출`

---

## 4. 마일스톤 12 — 보석십자수 심화

**목표**: 사진으로 도안 자동 생성 + 색 범례/필터 + 완성 연출. (M11 양자화 재사용)

**작업 순서**
1. 이미지 → 컬러 도안 자동 생성: `quantize.js`로 팔레트화 후 size에 맞춰 다운샘플하여 `pattern` 배열 생성. (기존 grayscale reference와 별개인 컬러 pattern)
2. 색 범례: 도안의 색별 **총/남은 개수** 목록. 색 항목 클릭 시 해당 색 필터.
3. 색 필터 모드: 선택한 한 색만 놓기(나머지 목표 칸은 잠금/흐림 표시) — 실제 보석십자수처럼 "한 색씩" 진행.
4. 완성 연출 강화: 색별 완료 표시, 전체 완성 시 축하 연출(과하지 않게). 진행률 세분(색별 진행 바).

**완료 기준**
- 사진 로드 → 도안 생성 → 색별로 채워 완성까지 흐름 동작
- 색별 개수(총/남은) 정확, 필터로 한 색만 놓기 동작
- 저장/복원(pattern) 정상, 큰 사이즈(64)에서 렌더 성능 확인
- §6 회귀 통과

**커밋**: `마일스톤 12: 보석십자수 심화 (사진→도안, 색 범례·필터)`

---

## 5. 마일스톤 13 — 내보내기 / 공유

**목표**: GIF·클립보드·스프라이트시트 메타데이터.

**작업 순서**
1. **[⚠ 사전 승인 필요]** 애니메이션 GIF 내보내기.
   - 옵션 A: 무의존 GIF89a 인코더 직접 구현(팔레트 ≤256이라 실현 가능).
   - 옵션 B: 라이브러리(gif.js 등) 추가 — **AGENTS.md: 의존성 추가는 사전 승인 필수**.
   - **착수 전 사용자에게 A/B 중 무엇으로 갈지 승인받고 시작한다. 임의 설치 금지.**
2. 클립보드 복사: 현재 프레임 PNG를 `navigator.clipboard`에 복사.
3. 스프라이트시트 + JSON 메타 동시 저장: 프레임 수/크기/FPS/프레임 위치 스키마.

**완료 기준**
- GIF가 프레임·속도(FPS) 정확히 재생됨(외부 뷰어 확인)
- 클립보드 복사 후 붙여넣기 됨
- 메타 JSON 스키마 정확·문서화
- §6 회귀 통과

**커밋**: `마일스톤 13: GIF·클립보드·스프라이트시트 메타 내보내기`

---

## 6. 회귀 체크리스트 (매 마일스톤 완료 시)

**기존(v1/v2 + 보석십자수)**
- 펜/지우개/채우기/스포이드/선택 정상 (마우스 + 터치)
- 단축키 B/E/G/I/M, Ctrl+Z, Ctrl+Shift+Z 정상
- 대칭 그리기, 그리드 토글 정상
- 16/32/64 크기 변경 시 초기화 + 히스토리 리셋
- 애니메이션 프레임(추가/복제/삭제/순서/어니언/재생), 스프라이트시트 PNG 폭 정확
- 타일 3×3 미리보기, 색칠공부(참조/투명도/경계 채우기)
- 프로젝트 .emberpix 저장/불러오기(손상 파일 안내 후 현재 유지)
- 보석십자수(도안 적용·가이드·페인트바이넘버·진행률·보석 렌더)
- PNG 4개 스케일 투명 배경 유지
- **undo가 현재 프레임에만 적용**
- 콘솔 에러 0건

**v3 신규(해당 마일스톤 이후 추가)**
- 확대/이동 후에도 모든 그리기 좌표 정확, 멀티터치 제스처와 그리기 충돌 없음
- 도형 미리보기가 픽셀 데이터를 오염시키지 않음
- 팔레트 저장/복원, 이미지 양자화 결과 안정
- GIF/클립보드/메타 출력 정확

## 7. 보고 형식 (매 마일스톤 완료 시, 한국어 3~5줄)

1. 무엇이 바뀌었나 (파일 단위)
2. 실행/확인 방법
3. 회귀 체크리스트 결과
4. 다음 마일스톤에서 결정 필요 사항 (있을 때만)

## 8. 금지 사항 (AGENTS.md 재확인)

- **의존성 추가는 사전 승인 필수** (특히 M13 GIF — 착수 전 방식 승인)
- 마일스톤 건너뛰기/병합 금지, 순서 준수
- 디자인 토큰(#141519 / #1e2027 / #ff7a2f) 임의 변경 금지
- 외부 UI 라이브러리 금지, 스타일 직접 작성
- 파일당 하나의 책임(코어 로직은 `src/core/*`, App.jsx는 UI·입력만)
- 애매한 정리/삭제 지시는 범위를 좁게 재확인한 뒤 실행
