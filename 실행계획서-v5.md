# Emberpix v5 실행 계획서 — M20 실기기 사용성 검증·재현 이슈 수정

> `AGENTS.md` → `ROADMAP.md` → 이 문서 순으로 읽는다. M1~M19는 완료했고, M20은
> 새 기능 마일스톤이 아니라 실제 폰 사용을 근거로 한 안정화 마일스톤이다.

## 1. 목표

아이들이 실제 폰에서 Emberpix를 시작하고, 그리고, 보석십자수를 하고, 작품을 보관한 뒤
다시 여는 흐름을 막힘없이 끝낼 수 있는지 확인한다. 재현 절차가 있는 문제만 고친다.

M16 컨페티는 2026-08-11 실기기에서 이미 확인됐다. M20에서 다시 구현하거나 변경하지 않는다.

## 2. 실행 명령

```powershell
npm run dev -- --host 127.0.0.1 --port 5173
node --test src/core/*.test.js
npm run build
git diff --check
```

## 3. 프로젝트 경계

| 영역 | 원칙 |
|---|---|
| 화면·입력 | `src/App.jsx`와 기존 직접 canvas/Pointer Events 구조를 유지한다. |
| 순수 로직 | 재현 이슈가 순수 함수로 분리되면 `src/core/`와 인접 `*.test.js`에 둔다. |
| 저장 | 자동저장·작품 보관함·`.emberpix` 포맷은 호환성을 깨지 않는다. |
| 디자인 | `#141519` / `#1e2027` / `#ff7a2f` 토큰과 현재 키즈 UI 밀도를 유지한다. |

## 4. 실기기 확인 순서

### A. 세로 화면

1. 시작 화면에서 첫 사용 안내를 읽고 그림 그리기·보석십자수 중 하나를 시작한다.
2. 그림 모드에서 펜/지우개/채우기/스포이드, Undo/Redo, 더보기 서랍을 사용한다.
3. 전체 지우기와 크기 변경에서 취소·확정을 각각 확인한다.
4. 보석 모드에서 내장 도안을 고르고 몇 칸을 채운다.

### B. 가로 화면

1. 캔버스가 정사각형이며 상단 바·조작부가 가려지지 않는지 확인한다.
2. 핀치 확대/이동 뒤 한 손가락 그리기가 의도치 않게 찍히지 않는지 확인한다.
3. 페이지 자체가 스크롤되지 않고, 스크롤은 서랍·가로 목록 안에서만 생기는지 확인한다.

### C. 저장 흐름

1. 작품을 보관함에 저장한다.
2. 앱을 새로 열어 시작 화면·자동저장 상태를 확인한다.
3. 내 작품에서 저장한 작품을 열고, 이름·썸네일·그림이 유지되는지 확인한다.

## 5. 이슈 처리 규칙

- 기록할 항목: 기기/브라우저/화면 방향, 행동 순서, 기대 결과, 실제 결과, 스크린샷 또는 짧은 영상.
- 같은 절차로 다시 일어나야 수정한다. 재현되지 않으면 기록만 남기고 코드를 추측해 바꾸지 않는다.
- 수정은 한 문제씩 작게 하고, 새 의존성·외부 UI 라이브러리·독립 기능은 추가하지 않는다.
- 각 수정 전후에 관련 Node 테스트, `npm run build`, 해당 브라우저 흐름을 확인한다.

## 6. 완료 기준

1. 세로·가로 실기기 테스트 결과가 있으며, 발견된 이슈는 재현 절차와 함께 정리됐다.
2. 재현 이슈는 수정·검증됐거나, 범위 밖인 이유가 명확히 기록됐다.
3. 수정이 있으면 회귀 테스트와 `npm run build`가 통과한다.
4. 확인하지 못한 기기·브라우저 조합은 통과로 적지 않는다.

## 7. 경계

- 항상: 한국어 보고/커밋, `.omx/` 비수정·비스테이지, 캔버스/undo 구조 보존.
- 먼저 물어볼 것: npm 의존성, 저장 포맷 변경, 배포·GitHub 설정 변경, 재현 없는 대규모 UX 개편.
- 절대 하지 말 것: 실제 작품·보관함을 테스트 편의상 삭제, 테스트 실패를 삭제로 숨기기, M21 기능을 M20에 섞기.

## 8. 커밋

M20의 재현 이슈 수정과 검증이 끝났을 때만 다음 제목으로 한국어 Lore Commit Protocol 커밋을 만든다.

`마일스톤 20: 실기기 사용성 검증 및 재현 이슈 수정`
