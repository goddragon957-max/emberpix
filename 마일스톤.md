# Emberpix 마일스톤

전체 마일스톤 이력과 예정 작업을 한곳에 모은 문서. **완료 여부의 정본은 git log다.**

- 룰: [AGENTS.md](AGENTS.md) · 방향: [ROADMAP.md](ROADMAP.md)
- 실행 계획서: [실행계획서-v3.md](실행계획서-v3.md)(M9~M13) · [실행계획서-v4.md](실행계획서-v4.md)(M17~M19)
- 골모드 프롬프트: [골모드-프롬프트-v3.md](골모드-프롬프트-v3.md) · [골모드-프롬프트-v4.md](골모드-프롬프트-v4.md)

## 한눈에

| 단계 | 마일스톤 | 상태 |
|---|---|---|
| v1 기준선 | 펜/지우개/채우기/스포이드, 대칭, undo, Sweetie 16, PNG 내보내기 | 완료 |
| v2 | M1~M7 — 구조·프레임·타일·색칠공부·프로젝트 파일·선택 | 완료 |
| 확장 | M8 — 보석십자수 | 완료 |
| v3 | M9~M13 — 확대/이동·도구 확장·팔레트·보석 심화·내보내기 | 완료 |
| 실사용 대응 | M14~M16 — 키즈 UI·PWA·반응형 | 완료 |
| v4 | M17~M19 — 배포·작품 보관함·다듬기 | 예정 |

---

## 완료

### v1 기준선 — `9d7fad1` (2026-07-10)
단일 `App.jsx`로 만든 검증된 출발점. 캔버스 16/32/64, 체커보드 투명 배경, 그리드 토글,
펜/지우개/채우기(플러드필)/스포이드, 좌우 대칭, 스냅샷 undo(최대 60), Sweetie 16 팔레트,
투명 PNG 내보내기(1/4/8/16×), 단축키 B/E/G/I·Ctrl+Z.

### M1 모듈 분리 + 자동 저장 — `256b2ac`
`App.jsx` 단일 파일을 책임별로 쪼갰다. `src/core/`(grid·history·renderer·exporter·storage) 신설,
`App.jsx`는 UI·입력 조립만 담당. localStorage 자동 저장/복구.

### M2 애니메이션 프레임 — `9a87331`
프레임 추가/복제/삭제/순서 변경, 어니언 스킨, FPS 재생, 가로 스프라이트 시트 PNG.
**히스토리를 프레임마다 독립**으로 둬서 undo가 현재 프레임에만 적용된다.

### M3 타일 모드 — `961370e`
3×3 반복 미리보기(`renderer.renderTilePreview`). 심리스 타일 확인용.

### M4 색칠공부 — `259d845`
참조 이미지를 흑백으로 다운샘플해 캔버스 아래에 깐다(`core/reference.js`). 내보내기에는 미포함.

### M5 색칠놀이 확장 — `bddf4db`
자작 16×16 라인아트 도안 갤러리(`core/templates.js`), 밑그림 투명도, 도안 선을 넘지 않는 경계 채우기.

### M6 프로젝트 저장/불러오기 — `36c23e3`
`.emberpix` JSON 포맷(`core/project.js`), 자동저장과 공유하는 검증 로직(`core/format.js`).
손상 파일은 안내 후 현재 상태 유지.

### M7 선택 도구 — `ed8dcdf`
사각 선택 → 이동/복사(Alt+드래그)/좌우반전/복제/삭제(`core/selection.js`). undo 연동.

### M8 보석십자수 — `5d03836`
픽셀을 보석알로 렌더 + 도안(pattern) 보고 톡톡 채우는 페인트-바이-넘버.
내장 도안 하트/별/꽃(`core/patterns.js`), 저장 포맷에 `pattern` 선택 필드 추가. 터치 우선.

### M9 캔버스 확대/이동 — `b09dd3b`
`core/view.js` — 휠 확대(커서 앵커), 두 손가락 핀치/팬. **포인터가 2개 이상이면 진행 중
스트로크를 취소하고 제스처로 전환**(확대하려다 점이 찍히는 사고 방지). 배율 1×~16×.

### M10 그리기 도구 확장 — `9c92672`
`core/shapes.js` — 직선(브레젠험)·사각형·원/타원, 브러시 크기 1~4, 전역 색 교체.
**도형 미리보기는 renderer 오버레이로만** 그려 픽셀 데이터를 오염시키지 않는다.

### M11 팔레트 관리 — `5a5cbfb`
`core/quantize.js`(미디언 컷 양자화·최근접 색), `core/palettes.js`(팔레트 슬롯·최근 12색).
이미지에서 대표색 추출. 내장 Sweetie 16은 저장하지 않고 런타임 0번 슬롯으로 붙인다(읽기 전용).
`.emberpix`를 v2로 올리고 v1의 평면 `palette` 배열은 사용자 슬롯으로 승격(하위호환).

### M12 보석십자수 심화 — `3702980`
`core/gems.js` — 사진→컬러 도안 생성(M11 양자화 재사용), 색 범례(색별 총/남은), 색 필터.
다운샘플 경계에서 생기는 구분 불가한 근사색은 큰 색에 흡수시킨다(`MERGE_DIST`).
renderer에 `patternFilter` 추가 — 선택한 색만 또렷하게, 나머지는 잠긴 것처럼 흐리게.

### M13 내보내기/공유 — `f45f8d7`
`core/gif.js` — **무의존 GIF89a 인코더 직접 구현**(전역 팔레트·투명 인덱스·LZW).
클립보드 PNG 복사, 스프라이트시트 + JSON 메타([스프라이트시트-메타.md](스프라이트시트-메타.md)).
> 함정: LZW 코드 폭 확장은 `next > (1 << codeSize)`여야 한다. 인코더 사전이 디코더보다
> 항상 한 항목 앞서기 때문. `===`로 바꾸면 디코더가 깨진다.

### M14 모바일·키즈 UI 개편 — `f2d545d`
아이들이 폰에서 쓸 때 화면이 밀리고 버튼이 아래로 몰리던 문제를 고쳤다.
`html/body` 고정(overflow hidden · position fixed · overscroll-behavior none)으로 페이지가
절대 스크롤되지 않게 하고, 상단바/캔버스/하단 조작부 3단 고정 레이아웃으로 재구성.
`core/modes.js` — 시작 화면에서 **그림 그리기 / 보석십자수** 모드 선택.
보석 모드는 펜 고정(지우개도 없음). 고급 기능은 "더보기" 서랍으로.

### M15 PWA — `b17dc43`
manifest + 무의존 서비스워커(문서는 네트워크 우선, 나머지는 캐시 우선 + 백그라운드 갱신).
`tools/make-icons.mjs` — Node 내장 zlib로 PNG를 직접 인코딩해 아이콘 생성(외부 패키지 없음).
홈 화면 설치·전체화면·오프라인.

### M16 반응형·하트 수정·컨페티 — `bc8d52e`
- **하트 도안 잘림 수정**: 하트 부등식의 실제 경계를 수치로 구해(x ±1.139, y −1.0~1.236,
  세로 중심 0.118) 격자에 맞췄다. 16×16 기준 136알 → 150알.
- **반응형**: 가로가 세로보다 길고 700px 이상이면 조작부를 옆 기둥으로. 폰 가로에서
  캔버스가 154×154 → 298×298.
- **정사각 보장**: `aspect-ratio` + `max-height`는 가로가 긴 화면에서 폭이 줄지 않아
  직사각이 된다(1158×479). `ResizeObserver`로 실측해 직접 지정.
- `core/confetti.js` — 완성 축하 컨페티를 외부 라이브러리 없이 직접 구현.
  방금 완성한 도안의 색을 뿌린다. `prefers-reduced-motion`이면 실행 안 함.

---

## 예정 — v4 ([실행계획서-v4.md](실행계획서-v4.md))

### M17 배포
`vite.config.js`에 `base: "/emberpix/"` + GitHub Actions 워크플로 → GitHub Pages.
HTTPS가 있어야 폰에서 홈 화면 설치·오프라인이 실제로 된다.
저장소 Settings → Pages 소스 지정은 **사용자가 직접** 해야 한다.

### M18 작품 보관함
지금은 자동저장 슬롯이 하나뿐이라 **새로 그리면 이전 그림이 사라진다.**
`core/library.js` — 여러 작품 저장·썸네일 목록·이어하기·삭제. localStorage 용량(약 5MB) 주의.

### M19 실사용 다듬기
내장 도안 확충(새 도안은 16/32/64 전부에서 잘림 확인 필수), 오조작 방지, 첫 사용 안내.

---

## 미해결 / 확인 못 한 것

- **M16 컨페티가 실제로 날아가는 모습을 아직 못 봤다.** 검증 환경의 브라우저 pane이
  `hidden`이면 `requestAnimationFrame`이 아예 돌지 않는다. 로직은 단위 테스트로 확인했고
  (입자 물리·결정론·전량 소멸), 캔버스 생성까지는 확인했다. M17 배포 후 실기기에서 확인할 것.
- **실제 아이 손으로 테스트한 적 없다.** 레이아웃 검증은 뷰포트를 강제해 수치로만 했다.

## 규칙 (마일스톤 진행 시)

- 마일스톤 단위로만 커밋. 커밋 메시지·보고는 한국어.
- 순서를 임의로 건너뛰거나 병합하지 않는다.
- **npm 의존성 추가 금지** — GIF도 컨페티도 아이콘 생성기도 직접 구현했다.
- 순수 로직은 `node`로 단위 테스트, `npm run build` 통과, 회귀 체크리스트 확인.
- 검증 못 한 항목은 통과했다고 쓰지 않는다.
