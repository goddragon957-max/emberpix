# Emberpix — Agent Rules (정본)

이 파일이 모든 도구(Codex/OpenCode 등)의 유일한 룰 소스다.
도구별 폴더에 룰을 복제하지 않는다.

## 프로젝트 정체성
- 브라우저 픽셀아트 에디터. Emberfall(브라우저 ARPG) 에셋 제작이 실사용 목표.
- Aseprite 클론이 아니다. 스프라이트/타일 제작에 필요한 최소 기능을 가볍게.

## 스택 / 제약
- Vite + React 단일 페이지. 외부 UI 라이브러리 금지, 스타일 직접 작성.
- 렌더링은 <canvas> 직접 제어. 픽셀 데이터는 ref 기반, 리렌더 최소화.
- Pointer Events 기반 입력, touch-action: none — 모바일 터치는 필수 지원.
- localStorage 자동 저장/복구 (마일스톤 3에서 프로젝트 파일로 확장).
- 디자인 토큰: 배경 #141519 / 패널 #1e2027 / 액센트 #ff7a2f. 임의 변경 금지.

## 워크플로우
- 답변/보고/커밋 메시지는 한국어.
- 마일스톤 단위 커밋. ROADMAP.md의 순서를 따르고 임의로 건너뛰지 않는다.
- 각 마일스톤 완료 시: 실행 방법 + 확인 포인트를 짧게 보고.
- 기능 추가 전 기존 도구/단축키(B/E/G/I, Ctrl+Z, Ctrl+Shift+Z) 회귀 확인.
- 의존성 추가는 사전 승인 필요. "일단 알아봐" 요청은 비침습 조사만, 설치 금지.

## 코드 규칙
- 파일당 하나의 책임. 캔버스 렌더러 / 입력 처리 / 히스토리 / 내보내기 분리.
  (현재 src/App.jsx는 검증된 v1 단일 파일 기준선 — 마일스톤 1 착수 시 분리 리팩터링부터.)
- 픽셀 좌표계는 (x, y), 인덱스는 y * size + x 로 통일.
- 스냅샷 기반 undo (최대 60), 스트로크 시작 시 1회 push.
