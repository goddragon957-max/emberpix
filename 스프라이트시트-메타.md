# 스프라이트시트 메타 JSON 스키마

Emberpix의 **시트+메타** 내보내기는 PNG 한 장과 같은 이름의 `.json` 한 개를 함께 저장한다.
게임(Emberfall) 쪽에서 프레임을 잘라 쓰기 위한 최소 정보만 담는다.

- 생성: `src/core/exporter.js`의 `sheetMeta(frames, size, scale, fps)`
- 좌표 단위: **출력 PNG의 픽셀** (셀 좌표 × `scale`)
- 배치: 현재는 가로 1열 고정 (`rows: 1`)

## 필드

| 필드 | 타입 | 설명 |
|---|---|---|
| `app` | string | 항상 `"emberpix"` |
| `version` | number | 스키마 버전. 현재 `1` |
| `image` | string | 짝이 되는 PNG 파일 이름 |
| `cellSize` | number | 원본 그리드 한 변 (16 / 32 / 64) |
| `scale` | number | 내보내기 배율 (1 / 4 / 8 / 16) |
| `frameWidth` | number | 프레임 한 장의 출력 폭 = `cellSize × scale` |
| `frameHeight` | number | 프레임 한 장의 출력 높이 (정사각이라 `frameWidth`와 같음) |
| `sheetWidth` | number | 시트 전체 폭 = `frameWidth × frameCount` |
| `sheetHeight` | number | 시트 전체 높이 = `frameHeight` |
| `columns` | number | 가로 프레임 수 (= `frameCount`) |
| `rows` | number | 세로 줄 수. 현재 항상 `1` |
| `frameCount` | number | 프레임 개수 |
| `fps` | number | 재생 속도. 0 이하로 들어오면 `8`로 보정 |
| `durationMs` | number | 프레임 한 장의 표시 시간 = `round(1000 / fps)` |
| `totalDurationMs` | number | 한 바퀴 재생 시간 = `durationMs × frameCount` |
| `frames[]` | array | 프레임별 잘라내기 정보 |

### `frames[]` 항목

| 필드 | 설명 |
|---|---|
| `index` | 0부터의 프레임 번호 |
| `x`, `y` | 프레임 좌상단 좌표 (`y`는 항상 0) |
| `w`, `h` | 프레임 크기 (`frameWidth` / `frameHeight`와 동일) |
| `durationMs` | 이 프레임의 표시 시간 |

## 예시 — 16×16, 2프레임, 8× 배율, 8fps

```json
{
  "app": "emberpix",
  "version": 1,
  "image": "emberpix_16x16_2f_8x.png",
  "cellSize": 16,
  "scale": 8,
  "frameWidth": 128,
  "frameHeight": 128,
  "sheetWidth": 256,
  "sheetHeight": 128,
  "columns": 2,
  "rows": 1,
  "frameCount": 2,
  "fps": 8,
  "durationMs": 125,
  "totalDurationMs": 250,
  "frames": [
    { "index": 0, "x": 0,   "y": 0, "w": 128, "h": 128, "durationMs": 125 },
    { "index": 1, "x": 128, "y": 0, "w": 128, "h": 128, "durationMs": 125 }
  ]
}
```

## 쓰는 쪽 예시

```js
const meta = await (await fetch("emberpix_16x16_2f_8x.json")).json();
const sheet = new Image();
sheet.src = meta.image;

// index번 프레임을 (dx, dy)에 그린다
function drawFrame(ctx, index, dx, dy) {
  const f = meta.frames[index];
  ctx.drawImage(sheet, f.x, f.y, f.w, f.h, dx, dy, f.w, f.h);
}
```

## 불변 규칙

- `frames[i].x === i × frameWidth`, `frames[i].y === 0`
- 마지막 프레임의 `x + frameWidth === sheetWidth`
- 배경은 투명(알파 0)이며 메타에는 배경색 정보가 없다
- PNG는 `imageSmoothing`을 끄고 그리므로 픽셀 경계가 보존된다 — 확대 시 `image-rendering: pixelated` 사용
