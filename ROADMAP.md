# Emberpix 로드맵

## 목표

브라우저에서 도는 가벼운 픽셀아트 에디터. Aseprite 대체가 아니라 필요한 최소 기능을 빠르게.

두 갈래 목적이 있고, 둘 다 살린다.

1. **Emberfall(브라우저 ARPG) 에셋 제작** — 스프라이트/타일, 애니메이션 프레임, 스프라이트시트·GIF 내보내기
2. **아이들이 폰으로 노는 그림 앱** — 그림 그리기 / 보석십자수 두 모드

## 방향을 정하는 기준

**실사용자는 폰을 쓰는 아이들이다.** 기능을 더 넣는 것보다 "지금 쓸 것만 크게 보이기"가 우선한다.
새 기능을 검토할 때 이 순서로 따진다.

1. 아이가 이걸 보고 뭘 해야 하는지 아는가?
2. 평소 화면을 복잡하게 만드는가? → 그렇다면 "더보기" 서랍으로
3. 어느 모드에 속하는가? (`src/core/modes.js`) — 모드 밖 기능은 보이지 않아야 한다
4. 실수로 작업이 날아갈 여지가 있는가?

## 지켜온 제약

- Vite + React 단일 페이지. **외부 UI 라이브러리·npm 의존성 금지** —
  GIF 인코더도 컨페티도 아이콘 생성기도 직접 구현했다.
- 렌더링은 `<canvas>` 직접 제어. 픽셀 데이터는 ref 기반, 리렌더 최소화.
- Pointer Events + `touch-action: none`. 모바일 터치는 필수.
- **페이지는 절대 스크롤되지 않는다.** 스크롤은 서랍·가로 목록 안에서만.
- 디자인 토큰: 배경 `#141519` / 패널 `#1e2027` / 액센트 `#ff7a2f`.

## 진행 상황

마일스톤 이력과 예정 작업은 **[마일스톤.md](마일스톤.md)** 에 정리돼 있다.
완료 여부의 정본은 `git log`다.

현재: **M1~M19 완료**, 다음은 **M20 실기기 사용성 검증·재현 이슈 수정**
([실행계획서-v5.md](실행계획서-v5.md)).

## 작업 규칙

- 마일스톤 단위로 커밋. 커밋 메시지·보고는 한국어.
- 각 마일스톤 완료 시 실행 방법과 확인 포인트를 짧게 보고.
- 기능 추가 전 기존 도구/단축키 회귀 확인.
- 검증하지 못한 항목은 통과했다고 쓰지 않는다.
