# Emberpix 골모드 프롬프트 (M11 → M13 완주용)

아래 블록을 그대로 복사해 새 세션에 붙여 넣으면 된다.

---

## 붙여넣을 프롬프트

너는 Emberpix(브라우저 픽셀아트 에디터) 프로젝트를 골모드로 이어서 완주한다.
작업 디렉터리: `C:\ProjectsSY\emberpix`

### 1. 먼저 읽어라 (순서대로)

1. `AGENTS.md` — 룰 정본
2. `ROADMAP.md` — 방향
3. `실행계획서-v3.md` — **실행 순서·완료 기준 (이 문서가 지시서다)**
4. `.agents/skills/canvas-editor/SKILL.md` — 캔버스/입력/히스토리 패턴과 함정

### 2. 현재 상태 (2026-07-30 기준)

- 브랜치 `master`, working tree 깨끗. 최신 커밋 `9c92672 마일스톤 10`
- **완료: M1~M10** — 모듈 분리+자동저장 / 애니메이션 프레임 / 타일 미리보기 / 색칠공부 / 색칠놀이 확장 / 프로젝트 저장·불러오기(.emberpix) / 선택 도구 / 보석십자수 / 캔버스 확대·이동 / 도형·브러시·색교체
- **남은 작업: M11 → M12 → M13** (실행계획서-v3.md의 §3, §4, §5)
- 코어 구조: `src/core/`(grid, history, renderer, exporter, storage, format, project, selection, reference, templates, patterns, view, shapes) + `src/App.jsx`(UI·입력 조립만)

### 3. 이번 실행 범위

**M11 → M12 → M13을 순서대로 끝까지 진행한다.** 각 마일스톤마다:

1. 구현
2. 순수 로직은 `node --input-type=module`로 단위 테스트 (결정론적이고 빠르다)
3. `npm run build`로 빌드 통과 확인
4. 실행계획서 §6 회귀 체크리스트 확인
5. **마일스톤 단위로 한국어 커밋** (계획서에 적힌 커밋 메시지 사용)
6. 계획서 §7 보고 형식(한국어 3~5줄)으로 보고한 뒤 **다음 마일스톤으로 바로 넘어간다**

마일스톤 사이에서 승인을 기다리지 말고 계속 진행한다. 단, 아래 §6 중단 조건에 걸리면 멈추고 물어라.

### 4. 이미 내려진 결정 (다시 묻지 마라)

- **M13 GIF는 외부 라이브러리 없이 직접 구현한다.** `src/core/gif.js`에 GIF89a 인코더를 작성한다(팔레트 ≤256이라 픽셀아트에 적합). **npm 패키지를 추가하지 마라.**
- M11의 `quantize.js`는 M12의 사진→도안 생성이 재사용한다. M11에서 견고하게 만들어라.
- 확대 배율 하한은 1×(화면 맞춤)이다. 그보다 축소하지 않는다.

### 5. 이 환경의 검증 방법 (중요 — 시간 낭비 방지)

- **스크린샷 도구를 쓰지 마라.** `computer{action:"screenshot"}` / `preview_screenshot`은 이 환경에서 30초 타임아웃된다.
- 브라우저 검증은 `mcp__Claude_Browser__javascript_tool`로 한다. **`tabId` 인자 필수**(빠뜨리면 InputValidationError).
- 캔버스 확인은 `ctx.getImageData`로 셀 중앙 색을 샘플링한다. 기준색: `#ef7d57` = `[239,125,87]`, 체커 배경 = `[35,37,45]` 또는 `[27,29,35]`.
- **오버레이가 샘플색을 바꾼다**: 선택 영역은 ember 10% 틴트, 도형 미리보기는 알파 0.8, 보석 가이드 점은 알파 0.3. 순수 색을 확인할 때는 `Esc`로 선택/미리보기를 해제한 뒤 샘플링하라.
- **브라우저 pane이 접혀 viewport가 0×0이 될 수 있다**(Electron `sandboxed_renderer` 오류). 이때 포인터 구동 검증은 불가능하다. 대안: dev 서버에서 모듈을 동적 import해 오프스크린 캔버스로 실제 코드를 검증하라.
  예: `const {render} = await import('/src/core/renderer.js')` → `document.createElement('canvas')`에 렌더 → `getImageData`로 검사.
- 포인터 조작은 합성 `PointerEvent` 디스패치로 한다. `setPointerCapture`는 합성 이벤트에서 예외가 날 수 있어 이미 try/catch로 감싸져 있다.
- **`git commit`이 2분을 넘길 수 있다**(dev 서버 파일 감시 영향). Bash 타임아웃을 240000ms로 넉넉히 잡아라. 타임아웃이 나도 커밋이 뒤늦게 성공할 수 있으니, 재시도 전에 `git log --oneline -3`으로 반드시 확인하라.
- 검증 불가한 항목이 있으면 **통과했다고 쓰지 말고, 무엇을 못 했는지 정직하게 보고하라.**

### 6. 중단 조건 (여기서만 멈추고 물어라)

- 의존성을 추가해야만 풀리는 문제에 부딪힌 경우 (GIF는 §4에서 직접 구현으로 이미 결정됨)
- 회귀 체크리스트가 깨지고 원인을 못 찾은 경우
- 계획서의 완료 기준을 만족시킬 수 없어 범위를 줄여야 하는 경우
- 파일/폴더를 삭제하거나 덮어써야 하는 경우 — 범위를 좁게 확인받은 뒤 실행

### 7. 반드시 지킬 것

- 답변·보고·커밋 메시지는 **한국어**
- 마일스톤 건너뛰기/병합 금지, 계획서 순서 준수
- 디자인 토큰 고정: 배경 `#141519` / 패널 `#1e2027` / 액센트 `#ff7a2f`
- 외부 UI 라이브러리 금지, 스타일 직접 작성
- 파일당 하나의 책임: 알고리즘은 `src/core/*`, `App.jsx`는 UI·입력만
- 픽셀 좌표는 `(x, y)`, 인덱스는 `y * size + x`
- **undo 스냅샷 대상은 "현재 프레임"** (프레임별 독립 히스토리). 프레임 전환이 다른 프레임 히스토리를 건드리면 안 된다
- 스트로크 = down에서 `pushUndo` 1회 → move에서 paint → up에서 종료. 도형은 up에서 `pushUndo` 1회 후 확정
- **확정 전 미리보기는 절대 픽셀 데이터를 건드리지 않는다** (renderer 오버레이로만)
- 좌표 계산에 `Infinity`/`NaN`이 들어가면 무한 루프가 날 수 있다 — 이미 방어돼 있으니 새 도형·좌표 로직에도 같은 가드를 넣어라
- 멀티터치: 포인터가 2개 이상이면 진행 중 조작을 취소하고 확대/이동 제스처로 전환 (`view.js` + `App.jsx`에 구현됨)

### 8. 시작

`M11`부터 시작해라. 실행계획서-v3.md §3을 그대로 따르고, 끝나면 곧바로 M12 → M13으로 이어가라.
