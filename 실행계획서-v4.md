# Emberpix v4 실행 계획서 (골 모드)

> **AGENTS.md(룰) → ROADMAP.md(방향) → 본 문서(실행 순서·완료 기준)** 순으로 읽고 착수한다.
> v3(M9~M13)는 `실행계획서-v3.md`에 있고 완료됐다. M14~M16은 실사용 피드백에서 나와
> 계획서 없이 진행됐다(git log 참조). 본 문서는 그 위에 얹는 **v4(M17~M19)** 다.
> 마일스톤 단위로만 커밋하고, 순서를 임의로 건너뛰거나 병합하지 않는다.

## 0. 현재 상태 (2026-08-06)

- 브랜치 `master`, 원격 `https://github.com/goddragon957-max/emberpix` (public, 기본 브랜치 master)
- 완료: **M1~M16**
  - M1~M8 모듈 분리·자동저장 / 프레임 / 타일 / 색칠공부 / 색칠놀이 / .emberpix / 선택 / 보석십자수
  - M9 확대·이동 · M10 도형·브러시·색교체 · M11 팔레트 관리 · M12 보석십자수 심화
  - M13 GIF·클립보드·시트 메타 · M14 모바일 키즈 UI(모드 분리) · M15 PWA · M16 반응형·컨페티
- 코어: `src/core/`(grid, history, renderer, exporter, storage, format, project, selection,
  reference, templates, patterns, view, shapes, quantize, palettes, gems, gif, modes, confetti)
  + `src/App.jsx`(UI·입력 조립만)
- **실사용자는 폰을 쓰는 아이들이다.** 기능 추가보다 "지금 쓸 것만 크게 보이기"가 우선.

**아직 눈으로 못 본 것**: M16 컨페티가 실제로 날아가는 모습.
브라우저 pane이 `hidden`이면 `requestAnimationFrame`이 아예 안 돌아 확인이 불가능했다.
M17 배포 후 실기기에서 반드시 확인한다.

---

## 1. 마일스톤 17 — 배포 (실기기에서 쓰게 만들기)  ⟶ 최우선

**목표**: 아이들 폰에서 실제로 홈 화면에 담아 쓸 수 있게 한다. HTTPS가 필요하므로 배포가 전제다.

**작업 순서**
1. `vite.config.js`에 `base` 추가. GitHub Pages는 `https://<user>.github.io/emberpix/`
   하위 경로라 `base: "/emberpix/"`가 없으면 에셋이 전부 404다.
   - manifest·서비스워커 경로는 이미 상대 경로(`./`)라 그대로 동작한다. **바꾸지 마라.**
2. GitHub Actions 워크플로 추가(`.github/workflows/deploy.yml`):
   master push → `npm ci && npm run build` → `actions/deploy-pages`.
   - **npm 패키지를 추가하지 마라.** 공식 액션만 쓴다.
3. 저장소 Settings → Pages 소스를 GitHub Actions로. **이건 사용자가 직접 해야 한다** —
   에이전트는 안내만 하고 기다린다.
4. 배포 후 확인: 앱 로드 / 아이콘 / manifest / 서비스워커 등록 / 오프라인.

**완료 기준**
- 배포 URL에서 앱이 정상 동작 (에셋 404 없음, 콘솔 에러 0)
- 폰에서 "홈 화면에 추가" → 주소창 없이 전체화면으로 뜸
- 비행기 모드에서도 실행됨
- **컨페티가 실제로 날아가는 것을 눈으로 확인** (M16 미확인 항목 해소)
- §5 회귀 통과

**커밋**: `마일스톤 17: GitHub Pages 배포 (base 경로, Actions 워크플로)`

---

## 2. 마일스톤 18 — 작품 보관함

**목표**: 지금은 자동저장 슬롯이 하나뿐이라 **새로 그리면 이전 그림이 사라진다.**
아이가 여러 작품을 만들고 다시 꺼내볼 수 있어야 한다. 기능적으로 가장 큰 구멍이다.

**작업 순서**
1. `src/core/library.js` 신규: 작품 목록 CRUD(생성/이름/삭제/불러오기), localStorage 기반.
   - 항목: `{ id, name, mode, size, updatedAt, thumb, data }` — `data`는 기존 저장 포맷 그대로.
   - `thumb`은 작은 PNG dataURL(예: 64px). 목록에서 그림으로 고르게 한다.
   - **용량 주의**: localStorage는 보통 5MB다. 64×64×여러 프레임이 쌓이면 넘친다.
     저장 실패(QuotaExceeded)를 반드시 처리하고, 상한(예: 20개)과 안내를 둔다.
2. 시작 화면에 "내 작품" 진입 추가 — 썸네일 격자에서 골라 이어 그리기.
3. 현재 작업 저장: 자동저장은 "작업 중" 슬롯 그대로 두고, 명시적으로 "보관함에 저장".
   새 작품 시작 시 현재 작업이 비어있지 않으면 저장 여부를 묻는다.
4. 보관함 항목 → .emberpix 내보내기/불러오기와 연결.

**완료 기준**
- 작품 여러 개 저장·목록·이어하기·삭제 동작, 새로고침 후에도 유지
- 용량 초과 시 앱이 죽지 않고 안내 후 현재 상태 유지
- 아이가 실수로 덮어쓰지 않음(새로 시작할 때 확인)
- §5 회귀 통과

**커밋**: `마일스톤 18: 작품 보관함 (여러 작품 저장·목록·이어하기)`

---

## 3. 마일스톤 19 — 실사용 다듬기

**목표**: 아이들이 실제로 걸리는 것들을 정리. **M17·M18을 실기기에서 써본 뒤** 착수한다.

**작업 순서**
1. 내장 도안 확충: 현재 3개(하트/별/꽃) → 자작 도안 몇 개 추가.
   **새 도안은 반드시 16/32/64 전부에서 가장자리에 닿지 않는지 확인한다**
   (하트가 잘려 있던 전례 — `patterns.js`의 HEART_* 상수 참고).
2. 오조작 방지: "전체 지우기"·크기 변경처럼 되돌리기 어려운 조작에 확인 단계.
3. 첫 사용 안내: 처음 들어온 아이가 뭘 눌러야 하는지 한 줄 힌트.
4. 실기기에서 나온 문제 반영.

**완료 기준**
- 새 도안 전부 잘림 없음(16/32/64)
- 실수로 작업이 날아가는 경로가 없음
- §5 회귀 통과

**커밋**: `마일스톤 19: 도안 확충·오조작 방지·첫 사용 안내`

---

## 4. 이미 내려진 결정 (다시 묻지 마라)

- **npm 의존성 추가 금지.** GIF 인코더도 컨페티도 직접 구현했다(`gif.js`, `confetti.js`).
  필요하면 같은 방식으로 직접 만든다. 예외는 GitHub 공식 액션뿐.
- **네이티브 앱 변환은 보류.** PWA로 간다(M15). 스토어 배포가 실제로 필요해지면 그때 재검토.
- 모드 분리: 시작 화면 카드 2개 → 그림 그리기 / 보석십자수.
  **보석 모드는 펜 고정**(지우개도 없음 — 되돌리기로 복구).
- 고급 기능은 "더보기" 서랍에. 평소 화면에 패널을 쌓지 않는다.
- **페이지는 절대 스크롤되지 않는다.** 스크롤은 서랍·가로 목록 안에서만.
- 확대 배율 하한은 1×(화면 맞춤).

## 5. 회귀 체크리스트 (매 마일스톤 완료 시)

**그리기 모드**
- 펜/지우개/채우기/스포이드/도형/선택 정상 (마우스 + 터치)
- 단축키 B/E/G/I/M/L/R/O/X, +/−, Ctrl+Z, Ctrl+Shift+Z
- 대칭, 그리드, 브러시 크기, 도형 미리보기가 픽셀 데이터를 오염시키지 않음
- 프레임(추가/복제/삭제/순서/어니언/재생), 타일 3×3, 색칠공부
- **undo가 현재 프레임에만 적용**

**보석 모드**
- 도안 적용·사진→도안·색 범례·색 필터(잠긴 칸은 안 놓임)·진행률
- 펜 고정(단축키로도 다른 도구가 안 켜짐)
- 완성 시 컨페티 1회, 이미 완성된 걸 열면 안 터짐

**공통**
- 16/32/64 크기 변경 시 초기화 + 히스토리 리셋
- 내장 도안이 어느 크기에서도 잘리지 않음
- PNG 4스케일 투명 유지, 스프라이트시트 폭, GIF 재생, 클립보드, 메타 JSON
- .emberpix 저장/불러오기(손상 파일 안내 후 현재 유지), 팔레트 복원
- **레이아웃**: 폰 세로/폰 가로/태블릿/데스크톱에서 캔버스가 정사각이고 화면이 넘치지 않음
- 멀티터치 제스처와 그리기 충돌 없음
- 콘솔 에러 0건

## 6. 보고 형식 (매 마일스톤 완료 시, 한국어 3~5줄)

1. 무엇이 바뀌었나 (파일 단위)
2. 실행/확인 방법
3. 회귀 체크리스트 결과 — **확인 못 한 항목은 통과했다고 쓰지 말고 못 했다고 쓴다**
4. 다음 마일스톤에서 결정 필요 사항 (있을 때만)
